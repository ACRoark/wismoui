/*
* {{ properCase name }} Messages
*
* This contains all the text for the {{ properCase name }} component.
*/

import { defineMessages } from 'react-intl';

export const scope = 'app.components.{{ kebabCase name }}';

export default defineMessages({
header: {
id: `${scope}.header`,
},
});
