import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.order-confirmation-view';

export default defineMessages({
  MIMO_START: {
    id: `${scope}.MIMO_START`,
  },
  MIMO_STOP: {
    id: `${scope}.MIMO_STOP`,
  },
  MIMO_TRANSFER: {
    id: `${scope}.MIMO_TRANSFER`,
  },
  viewStatusHistoryButtonText: {
    id: `${scope}.view-status-history-button-text`,
  },
});
