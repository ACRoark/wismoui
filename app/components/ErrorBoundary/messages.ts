import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.error-boundary';

export default defineMessages({
  backToMimoButtonText: {
    id: `${scope}.back-to-mimo-button-text`,
  },
  errorMessage: {
    id: `${scope}.errorMessage`,
  },
});
