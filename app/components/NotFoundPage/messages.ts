import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.not-found-page';

export default defineMessages({
  backButtonText: {
    id: `${scope}.back-button-text`,
  },
  message: {
    id: `${scope}.message`,
  },
  subtitle: {
    id: `${scope}.subtitle`,
  },
  title: {
    id: `${scope}.title`,
  },
});
