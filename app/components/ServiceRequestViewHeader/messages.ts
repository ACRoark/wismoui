import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.service-request-view-header';

export default defineMessages({
  orderNumber: {
    id: `${scope}.order-number`,
  },
  backToAllOrders: {
    id: `${scope}.back-to-all-orders`,
  },
  backToSearchPage: {
    id: `${scope}.back-to-search-page`,
  },
});
