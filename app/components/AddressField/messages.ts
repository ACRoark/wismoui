import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.address-field';

export default defineMessages({
  addressFieldLabel: {
    id: `${scope}.address-field-label`,
    defaultMessage: 'Address',
  },
});
