import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.search-view';

export default defineMessages({
  button: {
    id: `${scope}.button`,
  },
  name: {
    id: `${scope}.name`,
  },
  orderNumber: {
    id: `${scope}.order-number`,
  },
  title: {
    id: `${scope}.title`,
  },
});
