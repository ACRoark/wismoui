import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.billing-information';

export default defineMessages({
  authenticatedUsers: {
    id: `${scope}.authenticated-users`,
  },
  billingInformationLabel: {
    id: `${scope}.billing-information-label`,
  },
  guestPayLink: {
    id: `${scope}.guest-pay-link`,
  },
  MIMO_START: {
    id: `${scope}.MIMO_START`,
  },
  MIMO_STOP: {
    id: `${scope}.MIMO_STOP`,
  },
  signInLink: {
    id: `${scope}.sign-in-link`,
  },
});
