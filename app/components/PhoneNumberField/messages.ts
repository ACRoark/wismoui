import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.phone-number-field';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Contact Number',
  },
});
