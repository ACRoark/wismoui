import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.edit-service-date-link';

export default defineMessages({
  cancelButtonText: {
    id: `${scope}.cancel-button-text`,
  },
  continueButtonText: {
    id: `${scope}.continue-button-text`,
  },
  linkText: {
    id: `${scope}.link-text`,
  },
  promptMessage: {
    id: `${scope}.prompt-message`,
  },
  promptTitle: {
    id: `${scope}.prompt-title`,
  },
});
