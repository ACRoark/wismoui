import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.service-request-view';

export default defineMessages({
  serviceSummary: {
    id: `${scope}.service-summary`,
  },
  startTitle: {
    id: `${scope}.start-title`,
  },
  stopTitle: {
    id: `${scope}.stop-title`,
  },
});
