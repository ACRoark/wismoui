import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.verification-history';

export default defineMessages({
  AD: {
    id: `${scope}.AD`,
  },
  AN: {
    id: `${scope}.AN`,
  },
  AP: {
    id: `${scope}.AP`,
  },
  CL: {
    id: `${scope}.CL`,
  },
  DR: {
    id: `${scope}.DR`,
  },
  ES: {
    id: `${scope}.ES`,
  },
  FV: {
    id: `${scope}.FV`,
  },
  FW: {
    id: `${scope}.FW`,
  },
  LD: {
    id: `${scope}.LD`,
  },
  PA: {
    id: `${scope}.PA`,
  },
  PR: {
    id: `${scope}.PR`,
  },
  RE: {
    id: `${scope}.RE`,
  },
  RF: {
    id: `${scope}.RF`,
  },
  SC: {
    id: `${scope}.SC`,
  },
});
