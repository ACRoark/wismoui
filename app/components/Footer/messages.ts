import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.footer';

export default defineMessages({
  allRightsReserved: {
    id: `${scope}.all-rights-reserved`,
  },
  contactUs: {
    id: `${scope}.contact-us`,
  },
  privacyPolicy: {
    id: `${scope}.privacy-policy`,
  },
  termsAndConditions: {
    id: `${scope}.terms-and-conditions`,
  },
});
