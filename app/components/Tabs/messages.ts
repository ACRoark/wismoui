import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.tabs';

export default defineMessages({
  startService: {
    id: `${scope}.start-service`,
  },
  stopService: {
    id: `${scope}.stop-service`,
  },
});
