import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.header';

export default defineMessages({
  link: {
    id: `${scope}.link`,
  },
  serviceRequestsLink: {
    id: `${scope}.service-requests-link`,
  },
  title: {
    id: `${scope}.title`,
  },
});
