import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.completed-step';

export default defineMessages({
  MIMO_START: {
    id: `${scope}.MIMO_START`,
  },
  MIMO_STOP: {
    id: `${scope}.MIMO_STOP`,
  },
});
