import React from 'react';

import ColumnLayout from 'components/ColumnLayout';

const NotFoundView: React.FC = (): React.ReactElement => (
  <ColumnLayout>
    <div>Order not found</div>
  </ColumnLayout>
);

export default NotFoundView;
