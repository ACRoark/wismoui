import { defineMessages } from 'react-intl';

export const scope = 'wismo.components.summary-page';

export default defineMessages({
  backToMimoButtonText: {
    id: `${scope}.back-to-mimo-button-text`,
  },
  empty_message: {
    id: `${scope}.empty-message`,
  },
  system_unavailable_message: {
    id: `${scope}.system-unavailable-message`,
  },
});
